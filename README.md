GIT kick-off presentation
=========================

* Little presentation for coworkers on GIT
* Dependencies for building the presentation slides:
```
apt-get install pandoc
```
* Build the slides with
```
make
```
